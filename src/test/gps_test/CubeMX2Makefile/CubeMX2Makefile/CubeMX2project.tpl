(ede-cpp-root-project "$PRJ"
                      :name "$PRJ"
		      :spp-table '($C_DEFS)
      		      :compile-command "make"
                      :file "$PTH"
                      :include-path '($INCLUDES)
		      )